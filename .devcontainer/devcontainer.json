{
    "name": "dev-pytorch-fclip",
    "build": {
        // Path is relative to the devcontainer.json file.
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
        "--gpus",
        "all",
        "--ipc",
        "host"
    ],
    "mounts": [
        "source=/mnt/win_share, target=/nas, type=bind, consistency=cached"
    ]
}